функция дополнительная() {
    целое ф = 250;

    вернуть ф;
};

функция главная() {
    целое а5 = 158;
    считать(а5);
    вывести(а5);
    целое к = дополнительная();
    вывести(к);

    вернуть 0;
};
